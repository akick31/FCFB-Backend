package com.fcfb.arceus.service.fcfb.animation.choreography

/** Pre-snap alignment in yards: `depth` is off the line of scrimmage on that team's own side. */
data class Spot(
    val depth: Float,
    val lateral: Float,
)
