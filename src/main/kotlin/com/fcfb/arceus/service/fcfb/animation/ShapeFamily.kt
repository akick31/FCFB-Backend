package com.fcfb.arceus.service.fcfb.animation

enum class ShapeFamily {
    RUSH_ARC,
    PASS_ARC,
    INCOMPLETE_PASS,
    KICK_ARC,
    RETURN_ZIGZAG,
    STATIC_SNAP,
    ONSIDE_SCRAMBLE,
    BLOCKED_STUFF,
    FIELD_GOAL_ATTEMPT,
    KICKOFF_RETURN,
}
