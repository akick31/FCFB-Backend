package com.fcfb.arceus.service.fcfb.animation

enum class IncompletionKind {
    DROPPED,
    DROPPED_DEEP,
    BROKEN_UP,
    ALMOST_INTERCEPTED,
    ALMOST_PICKED_CROSSER,
    OVERTHROWN,
    THROWN_AWAY,
    SCREEN_WIDE,
}
